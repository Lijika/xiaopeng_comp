# Domain glossary — xiaopeng_comp (任务4)

## Terms

### 一致性审核工作台（Consistency Review Workbench）
以一笔**申请**为作业单元，汇集多份单据的字段证据、自动校验结论与人工处置记录的放款审核产品。  
_避免：规则引擎演示、JSON 校验器（它们只是候选技术能力，不是产品本身）。_

### 竞赛演示环境（Competition Demo Environment）
仅处理合成数据或不可逆脱敏数据、可面向公开展示的非生产环境；不得接收真实原始附件或可还原的个人与车辆信息。

### 受控部署环境（Controlled Deployment Environment）
在机构控制的私有网络或专属租户内处理真实原始附件与字段证据的产品环境；它与公开竞赛演示环境具有不同的信任边界。
_避免：生产模式（未表达机构控制与数据边界）。_

### 受限数据（Restricted Data）
原始附件、完整 OCR 文本、字段观察/证据、人工自由文本，以及可识别或重新关联具体个人、车辆或申请的数据；未知数据和派生数据默认继承其来源中的最高敏感级，除非经验证已不可逆匿名化。
_避免：业务数据、敏感信息（范围不明确）。_

### 受控显明（Controlled Reveal）
获授权角色为完成当前申请中的明确业务动作，在最小范围内查看原本脱敏的受限数据；显明行为需要留痕，且不自动授予下载或导出权限。
_避免：取消脱敏、查看原文（未表达授权范围与审计要求）。_

### 合规删除（Governed Deletion）
在保留期限届满或获授权的提前删除决定生效后，按版本化策略处置整个申请证据聚合及其派生副本，并仅保留不含业务原值的删除凭证；它不允许在有效保留期内改写历史事实。
_避免：硬删除、清理数据（未表达授权、范围和留痕）。_

### 受控导出（Controlled Export）
经明确业务目的、最小数据范围、接收方、有效期和独立授权约束，将数据带离工作台信任边界的可审计交付；查看或受控显明权限不自动包含该权限。
_避免：下载、生成报告（未表达数据离开信任边界后的控制）。_

### 受控外部集成（Controlled External Integration）
经机构逐项批准、以独立服务身份通过登记端点交换最小必要数据的系统连接，其处理目的、数据级别、保留、失败和退出边界均可审计；普通适配器或可联网代码不因此获得外部访问资格。
_避免：外部服务、联网能力（未表达默认拒绝和逐项授权）。_

### 放款审核员（Reviewer）
对一笔申请承担业务审核责任，并对自动校验发现的**不一致**与**存疑**项作出人工处置的人。  
_避免：用户、操作员（范围过宽）。_

### 规则管理员（Rule Administrator）
维护一致性校验政策、字段口径与实体变体知识草案的业务管理角色，不承担单笔申请的最终审核责任，也不能让草案直接影响运行时。

### 规则审批人（Policy Approver）
独立审查治理策略候选的业务语义、适用范围和影响并作出批准或驳回的角色；同一次发布的作者不得审批自己的候选，审批人也不能修改候选内容。

### 受治理配置草案（Governed Configuration Draft）
规则管理员在限定声明式模型内提出的校验政策、语义字段目录或实体知识变更；它不能包含可执行代码或外部访问，也不在批准并发布为不可变版本前影响校验。

### 治理策略候选（Governed Policy Candidate）
草案提交后按内容摘要冻结、等待自动验证和独立审批的不可变快照；任何内容修改都形成新候选并使旧验证与批准不再适用。

### 发布验证证据包（Release Validation Evidence）
绑定治理策略候选摘要、记录结构兼容、安全基线、语义约束、确定性、回归结果及数据轨限制的不可变验证集合；未覆盖范围与失败和通过结果同样保留，供审批追溯。

### 语义字段目录（Semantic Field Catalog）
版本化登记稳定字段标识、类型、单位或时间粒度、适用单据角色及显示/来源别名的业务词汇；既有标识的含义不得通过别名或原地编辑改变。

### 受保护控制基线（Protected Control Baseline）
独立于普通配置、任何规则或知识发布都不得削弱的最低资金安全与问责约束，包括权限边界、自动流转白名单、关键身份核验、证据与审计不变量。
_避免：关键规则配置（会误示规则管理员可以编辑或绕过该边界）。_

### 治理策略发布（Governed Policy Release）
把校验政策、语义字段目录、实体知识及适用的规范化、证据就绪和校验器版本按稳定标识与内容摘要原子绑定的不可变运行时选择单元；校验运行只能固定引用一个完整发布，不能拼接各工件的“当前最新版”。
_避免：规则版本（会遗漏知识、字段口径及其他共同决定运行语义的版本）。_

### 策略影响集（Policy Impact Set）
由策略工件依赖、发布适用范围和申请当前证据保守推导出的可能受治理策略变化影响的申请集合；它允许多纳入但不得漏项，并作为运行失效、重跑或显式追溯的依据。
_避免：重跑名单（会误示它可以由维护者手工挑选或只记录执行队列）。_

### 策略安全冻结（Policy Safety Hold）
在策略可信性存疑时对明确范围停止自动核验流转的失败关闭控制；它不修改结论或历史，且只能由显式恢复决定解除，不能到期自动恢复。

### 回滚发布（Rollback Release）
为恢复已知良好策略而新增的不可变激活记录，它在当前时间精确引用既有发布并重新产生影响集；回滚不删除坏版本、倒转历史或改写既有运行。

### 策略工件注册表（Policy Artifact Registry）
保存不可变策略组成工件、版本、内容摘要和兼容关系的治理事实所有者；它不拥有发布审批、激活状态或申请业务状态。

### 策略治理账本（Policy Governance Ledger）
保存候选验证与审批、发布调度与激活、安全冻结、回滚及重放等只追加治理事件的权威所有者；它向申请生命周期提供策略影响事实，但不能直接改写申请状态。

### 例外审批人（Exception Approver）
独立于提出例外的放款审核员，对真实存在的业务不一致是否允许例外作出授权决定的人。

### 集成方（Integrator）
负责把上游单据及结构化提取结果接入一致性审核工作台的系统或技术角色。

### 来源系统（Source System）
在一个机构或租户内登记、由获授权工作负载身份代表并向工作台提交业务申请及上游事实的系统；正文中的来源声明不能替代其身份绑定。

### 生产者（Producer）
实际生成页面分类、区域检测、字段候选或其他上游结果的任务、模型或系统；生产者可以不同于负责传输结果的来源系统，二者必须分别留痕。

### 来源适配器（Source Adapter）
把一种已登记来源格式确定性转换为规范提交信封的边界组件；它保留来源与适配版本及未映射信息，但不拥有认证、规范化、证据选择、冲突消解或申请状态。

### 审计员（Auditor）
在获授权的机构、时间与事件范围内只读检查审计事件和导出记录的监督角色，不承担申请处置、例外审批或规则维护责任。

### 平台管理员（Platform Administrator）
负责工作台身份接入、密钥、部署与存储运维的技术管理角色，默认无权查看申请业务内容。
_避免：系统管理员（未表达业务数据默认隔离）。_

### 原始附件（Source Attachment）
随申请提交、供审核员核对的不可变原始凭证文件或页图；它是审核证据，不等同于系统抽取出的字段值或**单据实例**。一份原始附件可以承载多份单据实例；补传、重扫或替换形成新版本，不覆盖旧材料。

### 附件清单（Attachment Manifest）
一次规范提交中对待接纳原始附件的完整声明，逐项绑定来源侧附件引用、受控内容位置、媒体类型、字节数、字节哈希与来源信息；清单项不是单据实例或字段事实。

### 受控对象引用（Controlled Object Reference）
指向已登记连接器及其信任边界内不可变对象版本的不透明引用；它不包含任意公网地址、调用方凭据或可自由解析的本地路径。

### 受控上传（Controlled Upload）
由工作台签发并绑定租户、来源、目标申请及预期内容属性的限时、限次材料传输；完成上传只形成可供规范提交引用的受控对象，不自动接纳为原始附件。

### 附件页（Attachment Page）
原始附件中的一个有序页图，其接收时页序不可原地修改。每个附件页只属于一个原始附件；它可通过**单据页归属**被一个或多个单据实例引用，也可暂时处于未归属状态。

### 接收页序（Received Page Ordinal）
附件页在原始附件容器中的固有顺序；它在接纳时固定，不等同于上游模型推断的页面类型或逻辑阅读顺序。

### 派生产物（Derived Artifact）
由原始附件或其他上游事实经过旋转、矫正、裁剪、识别或格式转换生成的不可变输出；它必须保留来源引用，不能覆盖或冒充原始附件。

### 单据页归属（Document-page Membership）
把一个附件页明确归入一个单据实例的只追加、可审计事实关系。它允许一份原始附件含多份单据实例，也允许一份单据实例跨多个原始附件或页；新增、撤销或重归属保留操作者、时间、理由与被取代关系，未建立该关系的页不得作为已归属单据字段证据参与自动校验。

### 单据实例声明（Document-instance Claim）
上游对一组材料可能构成某类、某角色逻辑业务单据的可追溯候选声明；它使用来源侧稳定引用，但在工作台接受并分配单据实例标识前不是单据实例事实。

### 单据页归属声明（Document-page Membership Claim）
上游对某附件页可能属于某单据实例及其页面类型、逻辑阅读位置的可追溯候选声明；多个声明可以并存，不能通过最后写入、最高置信度或多数票静默变成单据页归属事实。

### 未归属页（Unassigned Page）
已随申请接收、但尚未被归入任何单据实例的附件页。它保留为申请级原始证据，等待人工或上游重新归属，不能被自动校验视为某份单据的字段证据。

### 上游提取结果（Upstream Extraction Result）
任务一至三或外部识别系统从原始附件产生的页面分类、位置与候选字段值集合；候选值不是人工真值。

### 上游结果集（Upstream Result Set）
同一生产者针对一个任务、一次运行和一组固定输入形成的不可变观察集合；它保留原始产物与适配溯源，但不因此成为字段事实或字段证据。

### 生产者运行（Producer Run）
生产者在固定任务语义、模型或流水线版本、配置和输入范围上执行的一次可追踪处理；其身份、时间、结果和失败状态不得由适配器臆造。

### 规范提交信封（Canonical Submission Envelope）
集成方向工作台提交申请身份、材料、归属声明或上游提取结果时使用的唯一规范命令容器；来源格式必须先完成适配，信封本身不是申请状态、字段事实或证据快照。
_避免：Application JSON、上游原始包（会把演示模型或来源格式误作规范契约）。_

### 结构契约版本（Schema Contract Version）
标识规范提交信封及其类型化载荷的字段、类型和基数约束的不可变版本；结构可解析不代表业务语义兼容。

### 语义契约版本（Semantic Contract Version）
固定语义字段、状态、操作、关系和必解扩展含义的不可变版本；任何改变既有含义或默认行为的变化都必须发布新版本。

### 提交流（Submission Stream）
一个来源系统针对一笔上游业务申请及一种明确输入范围持续提交事实的有序边界；材料、归属和不同生产者任务可以属于不同提交流。

### 提交批次（Submission Batch）
提交流中共享来源修订和完整范围声明的一组规范提交信封；批内每封独立原子接纳，批次本身只有在显式结束后才可用于判断该范围是否已经提交完毕。

### 批次结束（Batch Closure）
来源系统通过最终序号、项目计数、清单摘要和范围声明明确表示一个提交批次不再接收成员的事实；它不等同于证据就绪，也不能由无流量或进程退出推断。

### 等待前驱（Awaiting Predecessor）
结构与完整性有效的规范提交因其声明的前一来源修订尚未接纳而停留的接入隔离状态；它尚未产生申请、证据或生命周期事实，也不能通过跳过序号自动解除。

### 接入隔离（Intake Quarantine）
已认证提交中尚未通过内容安全、完整性、因果或引用门槛的材料被限制保存、等待恢复事实的状态；隔离内容不进入不可变证据图，也不改变申请或证据修订。

### 证据资格（Evidence Eligibility）
一个已记录观察能否在固定校验运行中被选为字段证据的独立判定；它要求归属、溯源、语义和定位满足适用政策，不能由单一置信分决定。

### 替代（Supersession）
新增一个项目并显式声明其成为某个旧项目的当前替代者的只追加关系；旧项目及其历史用途永久保留，替代不等同于原地覆盖。

### 来源撤回（Source Withdrawal）
来源系统在其权限范围内停止主张某个既有来源项目有效的只追加事实；它不删除项目、不影响其他来源事实，也不等同于申请取消。

### 字段观察（Field Observation）
由上游提取或人工录入产生的、只追加的单个字段候选。它保留原文或空值、语义字段、置信信息、产生者与版本、时间，以及原始附件页/区域定位；同一单据实例的同一语义字段可以有多个字段观察。

### 字段提取尝试（Field Extraction Attempt）
生产者针对固定语义字段和页/区域执行的一次可追踪处理；它可以产生一个或多个字段观察，也可以明确结束为未检测、失败或不适用声明，未执行和待处理不产生字段观察。

### 空字段观察（Empty Field Observation）
生产者明确输出长度为零原始字符串的字段观察；空白字符串不是空字段观察，二者都不同于缺失证据。

### 显式空值观察（Explicit-null Field Observation）
生产者明确输出无标量值 null 的字段观察；它只表达该次观察没有值，不能自动解释为缺失、未执行或不适用。

### 字段证据（Field Evidence）
在固定校验运行的证据快照中，被选用且已通过单据归属与可追溯性校验的字段观察引用。自动校验结论必须能回溯到它；选用新的观察不会改写既有字段观察、字段证据或校验运行。  
_避免：字段值（单独使用时会丢失证据语义）或把字段证据当作唯一候选。_

### 缺失（Missing Evidence）
在证据范围已经明确结束后，某个校验点所需的单据范围与语义字段内没有可选用字段证据的派生状态；它不是原文为空、显式 null、未检测、失败、低置信或冲突的同义词。

### 低置信（Low-confidence Evidence）
字段观察或字段证据虽存在，但其置信信息未达到适用校验点自动采信阈值的状态；它保留原始候选与溯源，不能被表达为缺失。

### 证据冲突（Evidence Conflict）
同一语义字段存在多个可选用字段证据，且在适用的标准化与比较口径下彼此不能同时成立的状态；它不同于缺失和单纯低置信，并应保留所有冲突候选。

### 规范实体（Canonical Entity）
申请范围内、由字段证据关联形成的核验对象，例如车辆或自然人/机构当事方。它不替代原始附件、单据实例或字段证据，也不是跨申请的客户、车辆或其他主数据。

### 实体链接（Entity Link）
把字段证据关联到申请内规范实体的只追加、可审计关系。它保留证据引用、标准化与匹配规则版本，并明确为已关联、歧义、未关联或冲突；低置信字段证据不得自动建立已关联关系，状态变更不覆盖历史关系。

### 实体知识库（Entity Knowledge Base）
版本化的字段别名、实体变体与比较辅助知识。它可支持标准化或实体链接，但不生成、覆盖或替代申请中的字段事实，也不承担跨申请主数据或全局身份解析。

### 自动校验结论（Automated Check Verdict）
工作台在单个**校验发现**中基于字段证据产生的“一致 / 不一致 / 存疑”判断；它不是放款审批结论，也不能被人工处置改写。

### 校验运行（Check Run）
工作台在固定的申请处理周期、字段证据快照、证据就绪政策、规则包、实体知识与校验器版本上执行校验所形成的不可改写结果集合；只有全部适用校验点均有终态发现且运行完成事实可靠生效时才算成功，重新校验产生新的运行。

### 当前校验运行（Current Check Run）
与申请当前处理周期、证据修订及生效版本完全匹配、作为当前核验路由依据的唯一成功校验运行；结果提交时发现任一固定输入已变化，该运行就不能成为当前依据。

### 失效校验运行（Stale Check Run）
曾经完成或在提交时已发现输入落后的校验运行；它永久保留其固定快照与结果，但不能驱动当前核验路由，也不能被改写为匹配新证据或新版本。

### 复现重放（Reproduction Replay）
以原证据快照、治理策略发布和校验器重建历史结果的只读运行；它只验证可重现性与完整性，不能覆盖原结果或改变申请状态。

### 反事实模拟（Counterfactual Simulation）
以候选或其他策略发布评价冻结历史证据、用于比较行为差异的隔离运行；模拟结果不能成为当前校验运行、核验路由或下游交付依据。

### 业务重新校验（Operational Re-evaluation）
由策略影响、证据变化、显式追溯或获授权重开触发，在当前或新处理周期形成新校验运行的业务动作；只有它可以经申请生命周期成为新的当前依据。

### 校验点（Check）
版本化的业务断言定义，规定所需单据角色、基数、关系、必核性和规则政策；它不保存某笔申请的具体结果。只有明确声明允许多份时，才可对多个目标实例分别形成校验发现。

### 校验发现（Check Finding）
在一个固定校验运行中，针对一个校验点及其具体证据范围形成的不可改写结果。它包含自动校验结论、原因及字段证据引用；同一校验点在同一运行中可以形成多个独立发现。找不到所需目标形成缺失发现，多个不能按角色与关系区分的目标形成歧义/存疑发现。

### 必核校验点（Mandatory Check）
当前申请中适用且全部校验发现均须满足可流转条件，申请才具备自动流转资格的校验点；任一未处理的不一致、存疑或缺失发现都不能被其他一致发现抵消。

### 不可豁免校验点（Non-waivable Check）
被规则政策标记为不得通过业务例外恢复流转资格的校验点；任何工作台角色都不能豁免，只有纠正证据或补齐材料并重新校验为“一致”才能解除阻断。

### 可选校验点（Advisory Check）
用于补充提示但不决定自动流转资格的校验点；其结论仍须保留并可供审核员查看。  
_避免：必核规则、关键规则（除非确实指必核校验点）。_

### 核验路由（Verification Routing）
工作台依据当前校验运行、有效人工处置、例外决定和政策版本统一派生的申请级后续流转指示，如进入下一审批环节、转人工复核或暂不可处理；它必须区分自动与人工完成，且不批准或拒绝放款。  
_避免：审批结论、放款决定。_

### 路由事件（Routing Event）
一次核验路由判定被申请生命周期接受后形成的不可改写事实，保留路由码、用户可见原因码及所依据的运行、人工处置、例外决定和政策版本；后续复算产生新事件而不覆盖旧路由。

### 用户可见原因码（User-visible Reason Code）
解释当前主阶段、路由或失败原因的稳定机器标识，并配套责任方和恢复动作；显示文本可本地化，但原因码不得改义，也不得泄露受限数据或未授权资源的存在性。

### 人工复核（Manual Review）
放款审核员基于可读原始附件和字段证据，对未能自动流转的校验点作出人工处置的业务活动。

### 人工核实（Manual Verification）
放款审核员依据可读原始附件对自动“存疑”的校验点作出的肯定人工处置；它不改写自动校验结论，也不计作自动化通过。

### 暂不可处理（Unprocessable）
因原始附件、归属、配置、策略、完整性、权威存储或留痕能力不可信，工作台当前无法支持可问责判断的核验路由；它必须携带稳定原因、恢复条件和恢复目标，是待修复状态而不是业务拒绝结论。

### 恢复事实（Recovery Fact）
能够被系统验证、证明暂不可处理的具体原因已经解除的只追加事实，例如补件核验、依赖对账或完整性复核结果；修复权限或人工声明本身不是恢复事实。

### 放款审批结论（Disbursement Decision）
由工作台之外的授权审批流程作出的最终放款批准或拒绝决定；任务四不拥有该权限。

### 人工处置（Review Disposition）
放款审核员对特定校验发现及其校验运行作出的业务判断和依据；它与系统自动生成的校验结论相互区分，不能改写该结论或其证据，并进入审计记录。

### 人工修正（Evidence Correction）
审核员依据原始附件对错误或缺失的字段观察、页归属或实体链接作出的更正；它新增事实并记录被取代关系，不改写既有证据或自动校验结论，并使依赖旧证据的当前运行及在途工作项失效。

### 补件请求（Supplement Request）
要求责任方为一笔申请补充、重扫或替换特定业务材料的生命周期工作项；新材料形成新的原始附件版本，只有通过完整性、申请归属和材料角色核对后才算满足请求。

### 业务例外（Business Exception）
在自动校验确认可豁免的真实不一致后，由例外审批人对特定处理周期、运行、发现、证据和政策上下文授权继续流转的独立决定；它不把系统“不一致”改写为“一致”，上下文变化或到期后不得沿用。

### 例外决定（Exception Decision）
例外审批人对一项业务例外作出的不可改写批准或驳回；决定必须与申请人职责分离，且只有仍在其固定有效范围内的批准才能参与当前核验路由。

### 审计事件（Audit Event）
对需要问责的业务或安全动作及其主体、时间、结果、理由、资源和策略上下文形成的不可改写事实记录，覆盖人工处置、身份与权限、受限数据访问、导出、外部访问、保全和删除；它不得包含受限数据原值，更正、撤销或失效只能追加后继事件表达。

### 申请（Application）
一笔放款/进件的逻辑容器，可挂多份**单据实例**及其原始附件；它是工作台中的核心作业单元，并在申请接纳时获得工作台自己的稳定标识。

### 上游业务申请引用（Upstream Application Reference）
已登记来源系统在一个机构或租户内赋予业务申请的稳定引用，并在该来源系统范围内唯一；它用于识别同一业务申请的重复接入，但不替代工作台申请标识，也不得跨权限范围泄露存在性。

### 命令幂等键（Command Idempotency Key）
集成方为一次逻辑命令提供、在租户、调用身份、动作和目标范围内稳定复用的标识；同键同内容返回原结果，同键不同内容是冲突，不能生成第二个业务事实。

### 接入尝试（Intake Attempt）
集成方请求建立一笔申请的一次可幂等识别尝试；在申请接纳前，它只有稳定回执和失败原因，不是申请，也不能留下半创建的申请状态。

### 提交回执（Submission Receipt）
接入边界为一次规范提交签发的稳定结果记录，保留幂等绑定、契约与校验版本、接入处分、原因和安全恢复动作；它可以关联申请及修订，但不是申请生命周期状态。

### 申请接纳（Application Admission）
申请标识、接入幂等绑定、首个生命周期事件和必要审计共同生效的边界；申请只在该边界之后正式存在，后续材料或证据问题不得回滚其身份。

### 申请生命周期（Application Lifecycle）
一笔申请从接入到完成或终止的业务状态及合法转换集合；它是申请当前状态的唯一权威，外部参与者只能提交命令或事实，不能直接改写状态。

### 申请处理周期（Application Processing Cycle）
一笔申请从首次接纳或一次重开开始，到核验已完成或已终止为止的一段可独立追溯过程；每个周期拥有自己的当前运行、路由和人工处置上下文，旧周期不得被新周期改写。

### 重开（Reopening）
在既有处理周期已经核验完成或终止后，由获授权事实显式创建新处理周期的动作；它保留旧周期全部结论，不把晚到证据、旧人工处置或旧例外静默并入当前依据。

### 生命周期事件（Lifecycle Event）
一次已被申请生命周期接受的、只追加且不可改写的状态转换事实；更正、撤销、失效或重开只能通过追加后继事件表达。

### 当前申请态（Current Application State）
由一笔申请的有序生命周期事件派生、可重建的当前业务状态及待办；它是查询投影，不是独立于申请生命周期的第二套事实。

### 主阶段（Primary Phase）
当前申请态中唯一且互斥的业务阶段，用来表明申请当前受哪一道门槛阻挡以及下一步责任所在；规范阶段为接入中、组装中、待证据、证据就绪、校验中、路由判定中、待人工复核、待人工修正、待补件、待例外审批、暂不可处理、核验已完成、终止中和已终止。

### 生命周期工作项（Lifecycle Work Item）
由申请生命周期创建和收束的上游处理、补件、人工复核、人工修正或例外审批任务；多个工作项可以并存并各自保留历史，但都不成为申请主阶段的另一状态所有者。

### 复核工作项（Review Work Item）
固定关联一个申请处理周期、当前校验运行及具体校验发现的人工复核责任单元；不同工作项可并行处理，但全部业务提交仍由申请生命周期仲裁。

### 工作项认领（Work Item Claim）
一名获授权人员在有限有效期内承担某个生命周期工作项的排他处置责任；认领可续期、释放或超时转派，且这些变化只追加留痕，不等同于申请主阶段。

### 待证据（Awaiting Evidence）
申请仍在等待已知且可能到达的材料、上游结果或人工归属，因而尚不能固定当前校验快照的主阶段；它不同于已经确认的证据缺失或暂不可处理。

### 证据就绪（Evidence Ready）
申请当前证据修订已经满足固定校验快照的条件；它只表示证据范围可被问责地评价，不表示材料齐全、字段一致或申请可以自动流转。

### 证据就绪政策（Evidence Readiness Policy）
用于区分仍在途的已知证据、已经可评价的缺失以及无法建立可信范围的异常的一组版本化业务条件；它决定何时可以固定校验快照，但不规定具体输入格式。

### 核验已完成（Verification Completed）
工作台已经基于当前有效证据、校验运行及必要人工处置形成可进入下一审批环节的核验路由，并封存本次申请处理周期；它不是放款批准或拒绝结论，后续变化只能显式重开新周期。

### 下游交付义务（Downstream Delivery Obligation）
核验完成时同步形成、要求把核验路由可靠送达下一审批环节的可追踪责任；义务已经建立不等于下游已经接收，实际送达状态不改写已完成周期。

### 终止中（Terminating）
工作台已停止启动新的业务处理，正在收束在途工作并执行必要补偿，尚未达到已终止条件的主阶段。

### 已终止（Terminated）
工作台已停止继续处理、在途工作已被收束并封存本次申请处理周期的主阶段；它表达申请在工作台内结束，不等同于拒绝放款，后续变化只能显式重开新周期。

### 申请取消（Application Cancellation）
获授权上游业务主体要求工作台停止继续处理一笔申请的受控终止动作；它先进入终止中以收束在途工作，不删除历史，也不表达放款拒绝。

### 样本 / 样本级（Sample）
官方数据中的一本机动车登记证书（`sample_id`，如 `JFL25P02L080310-01`），由多页影像组成。  
**样本级处理** = 对该本证的页序理解、关键字段提取、样本内校验与展示。

### 单据实例（Document Instance）
申请下的一份逻辑业务材料，如机动车登记证书、交强险保单、融资租赁合同、发票或身份证；同一类型可存在多个单据实例。它通过**单据页归属**引用其原始附件页，并通过**单据角色**及其与规范实体的关系被校验点定位，而不等同于任何原始附件。
_避免：单据（未指明实例时）或把单据实例等同于原始附件。_

### 单据角色（Document Role）
单据实例在一笔申请中的业务位置及其与申请内规范实体的关系，例如当前车辆登记证、当前保单或本次融资合同。它连同所需基数构成校验点选择目标单据实例的显式依据，而不是单据类型、输入顺序或置信度的隐式替代。

### 页（Page）
样本内的一页影像。类型含 **注册页**、**登记页**（版面的 `page_type`）。

### 区域 / 检测框（Region）
页上的标注框：`class_id` + `bbox` + 置信度。  
注册页三大框：`4` 登记摘要(大框)、`5` 转移摘要(大框)、`6` 车辆信息(大框)。  
关键编号：注册页 `8` 登记证书编号 ↔ 登记页 `10` 登记证书编号(数字)（**同语义**）。

### 字段值（FieldValue）
字段观察或校验运行快照中的原子值表示，如原文或标准化值；字段值本身不是独立的可校验证据。无原文的观察可表明位置被检测到，但不能单独完成值级一致性校验。

### 原始值（Raw Value）
生产者为一次字段观察实际输出且未经任务四改写的值及其词法表示；空白、字符形式、来源类型和单位文本都必须原样保留。

### 规范化结果（Normalization Result）
任务四在固定校验运行中依据已选字段观察及版本化规范化、配置和知识口径生成的不可变解释；它与原始值分离，不能回写字段观察或脱离观察独立成为证据。

### 真实样本轨（Real-sample Track）
只使用真实原始附件及其可追溯派生结果，验证真实链路能够支持的接入、字段证据、归属、核验和人工复核；当前仅有机动车登记证时只产生样本内证据，只有新增真实多单据申请和独立金标后才能产生真实跨单据指标。

### 受控场景轨（Controlled-scenario Track）
使用明确标记为合成或受控扰动的多单据申请，验证任务四跨单据行为与评估流程；绑定真实样本标识但字段值仍为合成的申请仍属此轨，其结果不得表述为真实跨单据性能。

### 应评校验机会（Evaluation-eligible Check Opportunity）
在正式评估运行前，由冻结政策、目标证据范围和评估清单共同确定的一次具体校验机会；预测为存疑、跳过、缺失或错误都不能取消其评估资格。

### 评估金标（Evaluation Ground Truth）
独立于被评系统产生、带证据或场景变换溯源的应评校验机会标签，规范状态为一致、不一致、不可判定和不适用；只有一致与不一致进入主性能指标。

### 自动化覆盖率（Automation Coverage）
金标为一致或不一致的全部应评校验机会中，系统自动给出一致或不一致结论的比例；存疑、跳过、缺失和运行错误均为未覆盖，不能从分母删除。

### 不一致未自动拦截率（Miss Rate）
金标为不一致的应评校验机会中，系统没有自动给出不一致结论的比例；它包括误判为一致、存疑、跳过、缺失和运行错误，用于防止以不作答隐藏漏报风险。

### 真实端到端评估视图（Real End-to-end Evaluation View）
从真实原始附件经过上游提取、规范接入和证据选择直至任务四结论的评估视图；上游缺失与错误保留在最终指标中，但须按层归因。

### 任务四条件评估视图（Task-4-conditional Evaluation View）
以人工核验的结构化字段证据或冻结受控字段为输入、仅评价任务四标准化、实体链接和校验行为的评估视图；它不证明 OCR 或真实端到端性能。

### 评估运行包（Evaluation Run Bundle）
固定一次正式评估全部数据清单与哈希、分区、金标、证据快照、策略发布、实现版本、环境、命令、逐项结果和统计结论的不可变重放单元。

### 契约一致性夹具（Conformance Fixture）
使用合成或不可逆匿名化内容固定输入形态、期望规范输出、接入处分和原因码的契约测试样本；它证明实现遵守输入契约，不证明识别准确率或真实跨单据性能。

### 可配置跨单据能力（Configurable Cross-document Capability）
允许规则管理员维护单据类型、字段口径、实体变体与校验政策的任务四正式能力，不以当前是否拥有对应真实单据样本为存在条件。
